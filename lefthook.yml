post-checkout:
  commands:
    install:
      run: npm install --silent
pre-commit:
  parallel: true
  commands:
    test:
      glob: "main/*"
      run: npm run test -- --silent --ci
commit-msg:
  commands:
    lint:
      run: npm exec lint-commit-message {1}