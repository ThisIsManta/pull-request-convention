{
  "name": "pull-request-convention",
  "version": "2.1.1",
  "author": "Anantachai Saothong <thisismanta@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "esbuild main/index.js --bundle --outfile=dist/index.js --platform=node --target=node12 --minify",
    "preversion": "npm run build && git add dist/*",
    "postversion": "git push --tags origin master"
  },
  "devDependencies": {
    "esbuild": "^0.12.20"
  },
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/github": "^5.0.0",
    "lodash": "^4.17.21"
  }
}
